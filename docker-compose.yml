services:
  jr-backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      JR_API_KEY: ${JR_API_KEY}
    volumes:
      - ./.env:/app/.env:ro
      - ./docs:/app/docs:ro
